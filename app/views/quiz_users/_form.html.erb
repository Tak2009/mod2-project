<%= form_with(model: quiz_user, local: true) do |form| %>
  <% if quiz_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quiz_user.errors.count, "error") %> prohibited this quiz_user from being saved:</h2>

      <ul>
        <% quiz_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :quiz_id %>
    <%= form.number_field :quiz_id %>
  </div>

  <div class="field">
    <%= form.label :answers %>
    <%= form.check_box :answers %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
